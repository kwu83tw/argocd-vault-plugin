apiVersion: v1
kind: ConfigMap
metadata:
  name: example-secret
  annotations:
    avp.kubernetes.io/path: prod/secret
data:
  mydata: <foo | jsonParse | jsonPath {.browsers.name}>
  spring.profiles.active: rod
